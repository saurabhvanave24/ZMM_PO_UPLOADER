projection;
strict ;
use draft;

define behavior for ZC_MASS_PO_UPLOAD //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action POUploader;

  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;
}